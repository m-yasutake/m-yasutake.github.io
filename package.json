{
  "name": "tomika-bike",
  "private": true,
  "version": "1.0.0",
  "description": "tomika.bike — lint tooling only. The site itself has no build step (see CLAUDE.md); scripts/ has its own package.json for the Node data-refresh scripts.",
  "scripts": {
    "lint": "eslint --config config/eslint.config.js js scripts"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "globals": "^15.14.0"
  }
}
